prop: method = newton, use_mcwf = F

ham:
* type = matrix, filename = H0.dat, real_op = F, n_surf = 4, &
  sparsity_model = banded, op_type = pot

dissipator:
* type = dissipator, filename = D1.dat, real_op = F, sparsity_model = indexed
