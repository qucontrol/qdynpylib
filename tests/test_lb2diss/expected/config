tgrid: t_start = 0.0, t_stop = 10_ns, nt = 1000

pulse:
* type = const, E_0 = 0.5_GHz, id = 1

ham: type = matrix, n_surf = 2, sparsity_model = indexed
* filename = h_drift.dat, op_type = potential, op_unit = GHz
* filename = h_ctrl.dat, op_type = dipole, op_unit = unitless, pulse_id = 1

ham:
* type = matrix, n_surf = 2, sparsity_model = indexed, label = dissipative, &
  filename = h_drift.dat, op_type = potential, op_unit = GHz

psi:
* type = const, a = 1.0, surf = 2

psi: label = dissipative, type = const, a = 0.5
* surf = 1
* surf = 2

dissipator:
* type = dissipator, label = dissipative, filename = D_dissipative.dat, &
  op_unit = GHz, sparsity_model = indexed

prop: method = newton
